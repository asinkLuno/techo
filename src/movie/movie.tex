% ──────────────────────────────────────────
% Movie / TV rating page
% Size is set by \EDITION; the CJK font by \CJKFONT in the generated wrapper.
% When \CJKFONT names a .ttf file rather than an installed family, \CJKFONTPATH
% (also from the wrapper) gives its directory so fontspec can load it from disk.
% Build: techo movie "<query>"
% ──────────────────────────────────────────
\documentclass[10pt, twoside]{article}

% ── Size ──
\input{../../src/sizes.tex}
\edef\PW{\Size{\EDITION}{PW}}
\edef\PH{\Size{\EDITION}{PH}}

% ── Page geometry ──
\usepackage[paperwidth=\PW mm, paperheight=\PH mm,
  margin=0mm,
twoside]{geometry}

% ── Packages ──
\usepackage{tikz}
\input{../../src/colors.tex}
\usepackage{fontspec}
\usepackage{xeCJK}
\setmainfont{3270 Nerd Font Mono}
\ifdefined\CJKFONTPATH
\setCJKmainfont{\CJKFONT}[Path=\CJKFONTPATH]
\else
\setCJKmainfont{\CJKFONT}
\fi
% ☆ (U+2606) is absent from 3270 Nerd Font Mono — render the rating
% stars via DejaVu Sans Mono.
\newfontfamily\starfont{DejaVu Sans Mono}

% ── Page style ──
\pagestyle{empty}
\setlength{\parindent}{0pt}
\setlength{\parskip}{0pt}

% ──────────────────────────────────────────
\begin{document}
\input{content.tex}
\end{document}
