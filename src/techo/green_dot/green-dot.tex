% Green Dot — single empty page with dot-grid background
% Size is set by \EDITION in each edition's generated wrapper.
\documentclass[10pt, twoside]{article}

% ── Size ──
\input{../../src/sizes.tex}
\edef\PW{\Size{\EDITION}{PW}}
\edef\PH{\Size{\EDITION}{PH}}

% ── Page geometry ──
\usepackage[paperwidth=\PW mm, paperheight=\PH mm,
  inner=\BINDING mm, outer=\RIGHTMARGIN mm,
  top=\DTOP mm, bottom=\DBOT mm,
twoside]{geometry}

% ── Packages ──
\usepackage{tikz}
\input{../../src/colors.tex}
\usepackage{xcolor}

% ── Background: dot-grid ──
\input{../../src/dot-grid.tex}

% ── Page style ──
\pagestyle{empty}
\setlength{\parindent}{0pt}
\setlength{\parskip}{0pt}

% ──────────────────────────────────────────
\begin{document}
\input{content.tex}
\end{document}
