% ──────────────────────────────────────────
% Xianzhang m5 — plain ruled notebook (67×105mm)
% Build: uv run python xgen.py m5-xianzhang && cd m5-xianzhang && xelatex m5-xianzhang.tex
% ──────────────────────────────────────────
\documentclass[10pt, twoside]{article}

% ── Size ──
\input{../sizes.tex}

% ── Page geometry ──
\usepackage[paperwidth=\Size{m5-xianzhang}{PW} mm, paperheight=\Size{m5-xianzhang}{PH} mm,
            margin=0mm,
            twoside]{geometry}

% ── Packages ──
\usepackage{tikz}

% ── Colors ──
\definecolor{ruleblue}{HTML}{6B9BD2}

% ── Red vertical margin line ──
\AddToHook{shipout/background}{%
  \begin{tikzpicture}[remember picture, overlay]
    \draw[red!60, line width=0.4pt]
      ([xshift=\Size{m5-xianzhang}{RedLine} mm] current page.south west) --
      ([xshift=\Size{m5-xianzhang}{RedLine} mm] current page.north west);
  \end{tikzpicture}%
}

% ── Page style ──
\pagestyle{empty}
\setlength{\parindent}{0pt}
\setlength{\parskip}{0pt}
\setlength{\topskip}{0pt}

% ── Ruled page macro ──
\newcommand{\ruledpage}{%
  \begin{tikzpicture}[remember picture, overlay]
    \foreach \n in {0, 1, ..., \Size{m5-xianzhang}{LastLine}} {
      \pgfmathsetmacro{\y}{-\Size{m5-xianzhang}{TopGap} - \n * 6}
      \draw[ruleblue!45, line width=0.3pt]
        ([yshift=\y mm] current page.north west) --
        ([yshift=\y mm] current page.north east);
    }
  \end{tikzpicture}%
  \null
}

% ──────────────────────────────────────────
\begin{document}
\input{content.tex}
\end{document}
