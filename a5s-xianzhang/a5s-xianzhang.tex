% ──────────────────────────────────────────
% Xianzhang a5s — plain ruled notebook (110×210mm)
% Build: uv run python xianzhang.py a5s-xianzhang && cd a5s-xianzhang && xelatex a5s-xianzhang.tex
% ──────────────────────────────────────────
\documentclass[10pt, twoside]{article}

% ── Size ──
\input{../sizes.tex}

% ── Page geometry ──
\usepackage[paperwidth=\Size{a5s-xianzhang}{PW} mm, paperheight=\Size{a5s-xianzhang}{PH} mm,
            margin=0mm,
            twoside]{geometry}

% ── Packages ──
\usepackage{tikz}

% ── Colors ──
\definecolor{ruleblue}{HTML}{6B9BD2}

% ── Red vertical margin line ──
\AddToHook{shipout/background}{%
  \begin{tikzpicture}[remember picture, overlay]
    \draw[red!60, line width=0.4pt]
      ([xshift=\Size{a5s-xianzhang}{RedLine} mm] current page.south west) --
      ([xshift=\Size{a5s-xianzhang}{RedLine} mm] current page.north west);
  \end{tikzpicture}%
}

% ── Page style ──
\pagestyle{empty}
\setlength{\parindent}{0pt}
\setlength{\parskip}{0pt}
\setlength{\topskip}{0pt}

% ── Ruled page macro ──
\newcommand{\ruledpage}{%
  \begin{tikzpicture}[remember picture, overlay]
    \foreach \n in {0, 1, ..., \Size{a5s-xianzhang}{LastLine}} {
      \pgfmathsetmacro{\y}{-\Size{a5s-xianzhang}{TopGap} - \n * 6}
      \draw[ruleblue!45, line width=0.3pt]
        ([yshift=\y mm] current page.north west) --
        ([yshift=\y mm] current page.north east);
    }
  \end{tikzpicture}%
  \null
}

% ──────────────────────────────────────────
\begin{document}
\input{content.tex}
\end{document}
