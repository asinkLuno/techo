% ──────────────────────────────────────────
% Xianzhang m5 — plain ruled notebook (67×105mm)
% Build: uv run python xianzhang.py xianzhang-m5 && cd xianzhang-m5 && xelatex xianzhang-m5.tex
% ──────────────────────────────────────────
\documentclass[10pt, twoside]{article}

% ── Size ──
\input{../sizes.tex}

% ── Page geometry ──
\usepackage[paperwidth=\Size{xianzhang-m5}{PW} mm, paperheight=\Size{xianzhang-m5}{PH} mm,
            margin=0mm,
            twoside]{geometry}

% ── Packages ──
\usepackage{tikz}

% ── Colors ──
\definecolor{ruleblue}{HTML}{6B9BD2}

% ── Red vertical margin line ──
\AddToHook{shipout/background}{%
  \begin{tikzpicture}[remember picture, overlay]
    \draw[red!60, line width=0.4pt]
      ([xshift=\Size{xianzhang-m5}{RedLine} mm] current page.south west) --
      ([xshift=\Size{xianzhang-m5}{RedLine} mm] current page.north west);
  \end{tikzpicture}%
}

% ── Page style ──
\pagestyle{empty}
\setlength{\parindent}{0pt}
\setlength{\parskip}{0pt}
\setlength{\topskip}{0pt}

% ── Ruled page macro ──
\newcommand{\ruledpage}{%
  \begin{tikzpicture}[remember picture, overlay]
    \foreach \n in {0, 1, ..., \Size{xianzhang-m5}{LastLine}} {
      \pgfmathsetmacro{\y}{-\Size{xianzhang-m5}{TopGap} - \n * 6}
      \draw[ruleblue!45, line width=0.3pt]
        ([yshift=\y mm] current page.north west) --
        ([yshift=\y mm] current page.north east);
    }
  \end{tikzpicture}%
  \null
}

% ──────────────────────────────────────────
\begin{document}
\input{content.tex}
\end{document}
